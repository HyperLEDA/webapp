{
  "name": "hyperleda-webapp",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "yarn workspace @hyperleda/web dev",
    "build": "yarn workspace @hyperleda/web build",
    "lint": "yarn workspace @hyperleda/web lint",
    "preview": "yarn workspace @hyperleda/web preview",
    "start": "node server.mjs",
    "test": "yarn workspace @hyperleda/web test",
    "test:watch": "yarn workspace @hyperleda/web test:watch"
  }
}
